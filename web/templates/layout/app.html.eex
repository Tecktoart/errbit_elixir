<!DOCTYPE html>
<html>
<head>
	<meta charset="utf-8">
	<meta name="viewport" content="width=device-width, initial-scale=1">
	<title>Lumino - Dashboard</title>
	<link rel="stylesheet" href="<%= static_path(@conn, "/css/app.css") %>">

	<!--Custom Font-->
	<link href="https://fonts.googleapis.com/css?family=Montserrat:300,300i,400,400i,500,500i,600,600i,700,700i" rel="stylesheet">
</head>
<body class="app header-fixed sidebar-fixed aside-menu-fixed aside-menu-hidden">
        <header class="app-header navbar">
            <button class="navbar-toggler mobile-sidebar-toggler d-lg-none mr-auto" type="button">☰</button>
            <a href="/" class="navbar-brand">
            </a>
            <button class="navbar-toggler sidebar-minimizer d-md-down-none" type="button">☰</button>

            <ul class="nav navbar-nav ml-auto p-2">
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle nav-link" data-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">
                        <span class="d-md-down-none"><%= @current_user.name %></span>
                    </a>
                    <div class="dropdown-menu dropdown-menu-right">
                        <a class="dropdown-item" href="#"><i class="fa fa-user"></i> Profile</a>
                        <a class="dropdown-item" href="#"><i class="fa fa-wrench"></i> Settings</a>
                        <%= link to: session_path(@conn, :delete, @current_user), class: "dropdown-item", method: "delete" do %>
                          <i class="fa fa-lock"></i>
                          Logout
                        <% end %>
                    </div>
                </li>
            </ul>

        </header>

        <div class="app-body">
            <div class="sidebar">
                <nav class="sidebar-nav">
                    <ul class="nav">
                        <li class="nav-item">
                           <%= link to: home_path(@conn, :index), class: "nav-link" do %>
                             <i class="icon-speedometer"></i>
                             Dashboard
                           <% end %>
                        </li>

                        <li class="nav-item nav-dropdown">
                            <%= link to: app_path(@conn, :index), class: "nav-link" do %>
                              <i class="icon-puzzle"></i>
                              Applications
                            <% end %>
                        </li>

                        <li class="nav-item nav-dropdown">
                           <a class="nav-link" href="#"><i class="icon-puzzle"></i>Errors</a>
                        </li>

                        <li class="nav-item nav-dropdown">
                           <a class="nav-link" href="#"><i class="icon-user"></i>Users</a>
                        </li>
                    </ul>
                </nav>
            </div>

            <!-- Main content -->
            <main class="main">
              <%= render @view_module, @view_template, assigns %>
            </main>
        </div>

        <footer class="app-footer">
            <a href="http://coreui.io">CoreUI</a> © 2017 creativeLabs.
            <span class="float-right">Powered by <a href="http://coreui.io">CoreUI</a>
            </span>
        </footer>
    <script src="<%= static_path(@conn, "/js/app.js") %>"></script>
</body>
</html>
